const config=window.AURAMAX_CONFIG||{};const status=document.querySelector('#portal-status');const setStatus=(m='',k='')=>{status.textContent=m;status.dataset.kind=k};const escapeHtml=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function start(){try{const{createClient}=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm');const client=createClient(config.supabaseUrl,config.supabasePublishableKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});const{data:{session}}=await client.auth.getSession();if(!session)return location.replace('/account');document.querySelector('#welcome').textContent=`Signed in as ${session.user.email}`;document.querySelector('#sign-out').onclick=async()=>{await client.auth.signOut();location.replace('/account')};const profile=JSON.parse(localStorage.getItem('auramax-web-profile')||'null');document.querySelector('#guide-profile').textContent=profile?`${profile.face} face · ${profile.body} body. Your guide is personalised on this device.`:'No guide profile selected yet.';const localLessons=JSON.parse(localStorage.getItem('auramax-web-saved-lessons')||'[]');const{data:remoteLessons}=await client.from('auramax_saved_lessons').select('lesson_id,lesson_title').eq('user_id',session.user.id).order('created_at',{ascending:false});const lessonNames=[...(remoteLessons||[]).map(x=>x.lesson_title),...localLessons].filter((v,i,a)=>a.indexOf(v)===i);document.querySelector('#saved-lessons').innerHTML=lessonNames.length?lessonNames.map(x=>`<div class="saved-item">${escapeHtml(x)}</div>`).join(''):'<p>No saved lessons yet.</p>';const{data:savedGallery}=await client.from('auramax_saved_gallery_items').select('gallery_item_id,auramax_gallery_items(title,category)').eq('user_id',session.user.id);document.querySelector('#saved-gallery').innerHTML=savedGallery?.length?savedGallery.map(x=>`<div class="saved-item"><b>${escapeHtml(x.auramax_gallery_items?.title||'Saved outfit')}</b><br><small>${escapeHtml(x.auramax_gallery_items?.category||'LookBook')}</small></div>`).join(''):'<p>No saved LookBook ideas yet.</p>';}catch(e){setStatus(e?.message||'Unable to load your dashboard.','error')}}start();
